<%= render "users/shared/error_messages", resource: resource %>
<div class="sign-box col-xs-6 col-xs-offset-3">
  <h2><strong>Sign up</strong></h2>

  <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <table class="table">
      <tr>
        <th>User Name</th>
        <td><%= f.text_field :name, autofocus: true, style: "width: 250px;" %></td>
      </tr>
      <tr>
        <th>Email</th>
        <td><%= f.email_field :email, autocomplete: "email", style: "width: 250px;" %></td>
      </tr>
      <tr>
        <th>Password</th>
        <td><%= f.password_field :password, autocomplete: "new-password", placeholder: "6 characters minimum", style: "width: 250px;" %></td>
      </tr>
      <tr>
        <th>Password Confirmation</th>
        <td><%= f.password_field :password_confirmation, autocomplete: "new-password", style: "width: 250px;" %></td>
      </tr>
    </table>
    <div class="actions">
      <%= f.submit "Sign up!", class: "btn btn-default common-btn" %>
    </div>
  <% end %>
  <br><br>
  <%= render "users/shared/links" %>
</div>